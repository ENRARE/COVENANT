# End-to-end test scaffold

**MVP:** Playwright is configured for later demonstration flows. COV-001 has no product workflow to exercise, so `pnpm test:e2e` intentionally exits non-zero with a not-implemented status instead of implying end-to-end readiness.
