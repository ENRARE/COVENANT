{
  "name": "@covenant/authority",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@covenant/spec": "workspace:*",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "viem": "^2.33.3"
  }
}
